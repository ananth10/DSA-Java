package com.example.myapplication.algorithm.searching

/**
 * ->The way BFS works which starts from root node then left to right across second level and left to right across third level until last level
 * ->BFS uses additional memory because to track the child node
 * */
fun main(){

}