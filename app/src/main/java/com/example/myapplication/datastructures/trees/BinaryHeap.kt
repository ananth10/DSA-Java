package com.example.myapplication.datastructures.trees


/**
 * A Binary heap
 * -Max heap and Min heap
 * - A binary heap the parent node value always greater than the child node
 * - binary heap really great doing comparative operation
 * - A beauty of binary search tree that they took up least amount of space possible
 * - We dont have to balance like binary search tree, because of they do left and right insertions
 *
 * - Lookup O(n)
 * - Insert O(log n)
 * - delete O(log n)
 *
 * --PRIORITY QUEUE--
 *
 * */

fun main(){


}

